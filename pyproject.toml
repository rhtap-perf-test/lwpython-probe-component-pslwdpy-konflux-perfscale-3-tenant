[project]
name = "lwpython-probe-component"
version = "0.1.0"
description = "Placeholder package for Konflux LWPython perf probe component repo"
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
